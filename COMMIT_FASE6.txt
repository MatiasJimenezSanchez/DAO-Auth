feat(progress): implementación del motor de progreso y tracking de habilidades

RESUMEN DE CAMBIOS:
Se completó la Fase 6 del proyecto, estableciendo el puente relacional entre Usuarios, Simulaciones y el Catálogo de Habilidades, permitiendo el tracking de progreso y la evaluación granular.

DETALLES TÉCNICOS:
1. Modelos (app/models/progress.py):
   - `UserSimulation`: Control de inscripciones (enrollment) y progreso general (%).
   - `UserTask`: Tracking granular por tarea, intentos y calificaciones.
   - `SimulationSkill`: Mapeo de pesos y relevancia de habilidades por simulación.
   - `UserSkill`: Sistema de progresión del usuario (XP y nivel dinámico por habilidad).
2. Schemas (app/schemas/progress.py):
   - Implementados `EnrollmentCreate`, `TaskSubmission` y `UserProgressOut`.
3. Base de Datos:
   - Migración autogenerada aplicada exitosamente.
4. QA y Testing:
   - Creada suite segura `test_progress_engine.py` validando instanciación y cálculo de experiencia (100% pass rate).
   - Se corrigieron las importaciones en `app/api/v1/progress.py`.
