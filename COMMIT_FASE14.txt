test(core): infraestructura de tests robusta con SQLite y NullPool

RESUMEN ARQUITECTÓNICO:
Se resolvió la deuda técnica de los tests de integración. Se implementó un entorno aislado usando `test.db` con `NullPool` para prevenir bloqueos de concurrencia y se habilitó un Global Seeder (Región, Empresa, Catálogos) dentro de un fixture `setup_db` (scope=function) para proteger la integridad referencial en toda la suite. 100% de los tests pasando (400+).

UPDATED:
- `tests/conftest.py`: Refactor completo a In-Memory/Local SQLite. Implementación de PRAGMA foreign_keys y Seeders integrados para módulos dependientes.
- `app/api/v1/content.py`: Inyección de Programación Defensiva para validación manual de llaves foráneas (Módulos/Tareas/Recursos) previas al commit.
- `tests/catalogs/test_catalogs.py`: Resolución de colisiones de nombre con el Global Seeder.
- `pytest.ini`: Aislamiento forzado de variables de entorno para usar `test.db` independiente de Docker Compose.
