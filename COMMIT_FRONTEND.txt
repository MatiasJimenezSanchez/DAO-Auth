feat(core): ecosistema masivo de datos para frontend y auth v2

RESUMEN DE ARQUITECTURA:
1. 🔐 Seguridad V2:
   - Migración completa a inicio de sesión por Email (en lugar de username).
   - Implementación de sistema de doble capa: Access Token + Refresh Token.
   - Nuevo endpoint `/api/v1/refresh` protegido contra abusos de tokens cruzados.

2. 🌌 Ecosistema de Datos (Mega-Seeder):
   - Se construyó un seeder idempotente ("Modo Bestia") a prueba de colisiones (IntegrityError blindado).
   - Inyección de 10 usuarios realistas con progreso avanzado (XP, niveles, rachas).
   - Creación de +25 Simulaciones con profundidad (generación automática de Módulos, Videos, Quizzes y Entregables).
   - Árbol de Habilidades poblado con +30 nodos técnicos y blandos.
   - Tablas de Comunidad (canales y mensajes) inyectadas vía SQL directo para habilitar la vista estilo Discord.

=====================================================
🚀 GUÍA DE IMPLEMENTACIÓN PARA EL FRONTEND
=====================================================
Hola equipo de Frontend 👋. El backend ya está escupiendo datos reales para sus vistas. Aquí tienen cómo mapear el diseño a la API:

🔑 1. CREDENCIALES DE PRUEBA (Para ver datos reales):
- Usen el usuario principal para pruebas de integración:
  -> Email: alexander.lopez@delphos.com
  -> Password: Campus2026!
- El endpoint `POST /api/v1/login/access-token` (enviando email en el campo username) les devolverá un `access_token` y un `refresh_token`.

👤 2. PERFIL Y ÁRBOL DE HABILIDADES (Min 1:52 del video):
- Consulten el endpoint del usuario actual (ej. `GET /users/me`).
- Mapeen los campos `xp_total` (14,500), `level_current` (Nivel 12) y `streak_days` (45 días) directo a la UI.
- Las habilidades para los nodos hexagonales se sacan del catálogo (categorías: technical, soft, tool, methodology).

🎯 3. TABLERO DE MISIONES (Min 1:16 del video):
- Consuman el catálogo de simulaciones.
- Filtren por `difficulty_level` (basic, intermediate, advanced) para clasificar entre misiones principales y secundarias.
- Las tarjetas ahora tienen `estimated_hours` y descripciones reales. Si el usuario hace clic en "Ver Detalles", el backend tiene Módulos y Tareas anidadas listas para renderizar.

💬 4. COMUNIDAD Y CHAT (Min 1:32 del video):
- Tienen disponibles canales como `#cloud-y-aws`, `#ui-ux` y `#general`.
- El backend ya tiene mensajes pre-cargados entre usuarios (Diego, Carlos, Maria, etc.) para que la vista del chat no se vea vacía al cargar.
