feat(simulations): endpoints core de catalogo de simulaciones y progreso (Fase 15)

RESUMEN ARQUITECTÓNICO:
Se implementó el corazón transaccional de la plataforma (B2C/SaaS). Los estudiantes ahora pueden consumir el catálogo de simulaciones e inscribirse. Se implementó una capa de validación defensiva en la capa de servicios para asegurar la integridad de cupos y aislamiento de datos entre usuarios. Suite de tests completada exitosamente.

CREATED:
- `app/services/progress_service.py`: Encapsula la lógica de negocio para inscribir usuarios (`UserSimulation`), deducir cupos y registrar el envío de tareas (`TaskSubmission`).
- `tests/api/test_simulations_endpoints.py`: Pruebas de integración E2E comprobando filtros de listado, validaciones de cupos y aislamiento de sesiones entre usuarios.

UPDATED:
- `app/schemas/simulations.py`: Consolidación de schemas (Pydantic V2).
- `app/schemas/progress.py`: Definición de `UserSimulationOut` y `EnrollmentResponse`.
- `app/api/v1/simulations.py`: Endpoints públicos (`GET /`) y protegidos (`POST /{id}/inscribir`).
- `app/api/v1/progress.py`: Endpoints para consultar mis inscripciones (`GET /users/me/simulations`).
- `app/services/simulation_service.py`: Lógica de listado con filtros y paginación.
