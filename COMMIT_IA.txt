feat(core): implementación de arquitectura Feature Store para IA

RESUMEN DE CAMBIOS:
Se ha preparado la base de datos relacional para actuar como fuente de verdad y Feature Store del futuro motor de Machine Learning y sistema de recomendación (Matchmaking).

DETALLES TÉCNICOS:
1. Tabla `users`: 
   - Añadidos descriptores académicos (nivel_educativo, campo_estudio, nombre_institucion).
   - Inyectados vectores cognitivos base (analytical_score, creative_score, social_score, linguistic_score, hands_on_score).
   - Integrado flag de telemetría (origen_datos) para control de pesos en ML.
2. Tabla `simulations`:
   - Desnormalización estratégica: Inyectado `industria_principal_id` directo en la tabla para evitar JOINs costosos durante la ingesta masiva de datos de entrenamiento.
3. Nuevo Modelo IA:
   - Creada tabla `recomendaciones_ia` para almacenar el output del algoritmo (engagement_probability) de forma asíncrona, optimizando la latencia del feed.
4. Seguridad:
   - Migración autogenerada aplicada (cffe39e02ec2).
   - 100% de test pass rate mantenido (184/184 tests exitosos).
