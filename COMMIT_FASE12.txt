feat(db-core): finalización absoluta Fase 12 - infraestructura y cierre DBML

RESUMEN ARQUITECTÓNICO:
Cierre definitivo de las brechas detectadas entre el diagrama DBML original y el Data Layer en código. Se implementaron los modelos de infraestructura periférica, auditoría de entidades, gamificación base y control de seguridad. Se resolvieron colisiones de importación en SQLAlchemy y dependencias circulares durante el testing.

CREATED:
- `app/models/infrastructure.py`: 11 modelos nativos (SocialAuth, UserSession, AuthLog, RateLimit, SystemConfig, Level, Referral, MentorSimulation, AuditSimulation, AuditCompany, AuditUser).
- `app/schemas/infrastructure.py`: Schemas Pydantic V2 (*Create y *Out) para todos los modelos de infraestructura.
- `tests/infrastructure/test_infrastructure_safe.py`: Suite robusta en memoria (61 tests exitosos) validando restricciones lógicas y esquemas.
- `alembic/versions/72c9aac5e073_fase12_infrastructure.py`: Migración atómica autogenerada y aplicada.

UPDATED:
- `app/models/__init__.py`: Reestructuración masiva para registrar los nuevos modelos en el metadata de Alembic e importación corregida de `SimulationSkill` desde el módulo progress.
- `app/models/infrastructure.py`: Implementación de `__table_args__ = {"extend_existing": True}` como blindaje contra el bug de doble importación de Pytest, y corrección de la FK circular explícita para `CompanyUser`.

DELETED:
- Eliminación del modelo duplicado `SimulationSkill` dentro de `infrastructure.py` (delegando el control al archivo original `progress.py`).
- Remoción de la clase `TestSimulationSkill` en la suite de pruebas para resolver el conflicto de nomenclatura bilingüe en SQLAlchemy caché.
