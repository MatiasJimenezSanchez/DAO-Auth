fix(enterprise): inyección de schemas faltantes y expansión de cobertura a 325+ tests

RESUMEN DE CAMBIOS:
Resolución de ImportError en la recolección de pruebas de la Fase 11 y ampliación masiva de la suite de tests de QA. Se prepararon todos los Data Transfer Objects (DTOs) de salida para la futura capa de Controladores (Routers).

DETALLES TÉCNICOS:
1. Pydantic Schemas (app/schemas/enterprise.py):
   - AÑADIDO: Esquema `SavedPostCreate` faltante.
   - AÑADIDO: 14 esquemas de salida (`*Out`) con `ConfigDict(from_attributes=True)` para serialización directa desde objetos SQLAlchemy.
   - CORRECCIÓN: Se ajustó `user_id` a `Optional[int]` en `FraudAttemptCreate` y `GeneralFeedbackCreate` para soportar acciones de usuarios anónimos (sesiones no autenticadas).

2. QA & Testing (tests/enterprise/test_enterprise_safe.py):
   - Expansión de 6 a más de 60 tests unitarios estrictos en memoria.
   - Validación profunda de Pydantic: Regex, límites numéricos (precios negativos, IDs en cero), validación de strings vacíos y longitudes máximas.
   - Verificación de asignación de Default values de SQLAlchemy en entornos SQLite.

ESTADO GLOBAL:
Ejecución de test suite global completada en contenedor Docker: 325 passed, 19 skipped. Estabilidad estructural confirmada al 100%.
