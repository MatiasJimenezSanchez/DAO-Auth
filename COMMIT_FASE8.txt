feat(analytics): implementación de telemetría y pipeline B2B (ATS)

RESUMEN DE CAMBIOS:
Se completó la Fase 8 del proyecto. Se preparó la base de datos para actuar como Data Lake inicial de eventos y se construyó el modelo relacional para conectar el talento (usuarios) con las empresas.

DETALLES TÉCNICOS:
1. Modelos (app/models/analytics.py):
   - `UserEvent`: Tabla de telemetría para ingesta de clickstream. Uso de JSON/JSONB agnóstico (JSONB en Postgres, JSON en SQLite para testing).
   - `Candidate`: Pipeline de reclutamiento (ATS) con tracking de origen, puntuación y estados de contacto.
2. Schemas (app/schemas/analytics.py):
   - Implementación de validaciones Pydantic para ingesta de eventos (`UserEventCreate`) y actualización de candidatos (`CandidateUpdate`).
3. Base de Datos:
   - Migración autogenerada aplicada exitosamente.
4. QA y Testing:
   - Creada suite `test_analytics_safe.py` con validación de inyección JSON y límites de puntuación.
